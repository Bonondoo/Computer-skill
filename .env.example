# Copy this file to .env and fill in your API keys (do NOT commit real secrets)
REACT_APP_GOOGLE_API_KEY=your_google_api_key_here
REACT_APP_IP_KEY=your_ipgeolocation_key_here

# Notes:
# - The project reads REACT_APP_GOOGLE_API_KEY in src/firebase.ts and src/Component/const/api.ts for Google/Firebase usage.
# - REACT_APP_IP_KEY is used for the IP Geolocation service.
# - PUBLIC_URL is set automatically by CRA; you generally don't need to set it.